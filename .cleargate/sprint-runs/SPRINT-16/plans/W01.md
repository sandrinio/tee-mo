role: architect

# W01 — SPRINT-16 Milestone Plan (EPIC-025 ×6 stories + CR-001 + BUG-001)

**Architect:** session 2026-04-25 · **Sprint branch:** `sprint/S-16` off `main@c94bc6e`
**Scope:** 8 items in one wave — six EPIC-025 stories (workspace v2 redesign), one P3 enhancement (CR-001 channel picker search), one P3 bug (BUG-001 nav glassmorphism).

> Sprint plan: `.cleargate/delivery/pending-sync/SPRINT-16.md`. Epic: `EPIC-025-workspace-v2-redesign.md`. Design contract: `design_handoff_workspace_redesign/README.md`.

---

## Executive summary

Replace the 1235-LOC stacked workspace settings route with a sticky-tab + scrollspy shell over six sequential stories (foundation → 4 group migrations → mobile + cutover), preserve every existing module's behavior verbatim, and pull in one same-file CR (channel search) and one independent BUG (nav polish). Sequencing is **025-01 first → 025-02..05 + BUG-001 in parallel → CR-001 after 025-02 → 025-06 last**; the route file and `moduleRegistry.ts` are red-zone surfaces touched by every EPIC-025 story. Highest-leverage decisions are the prop shapes for the four foundation primitives in 025-01 (§5) — get them wrong and four follow-on stories pay rework tax. **Two pre-flight verifications materially change story scope: BUG-001's "actual behavior" is stale (the fix is already on `main`), and STORY-025-05's premise about the DELETE handler is wrong (it does not call `assert_team_member`)** — both flagged in §7 Open questions.

---

## 0. Flashcard sweep (Developer must grep BEFORE coding)

Per-story tag bindings. Run `grep '#tag' .cleargate/FLASHCARD.md` for each.

- **025-01:** `#frontend`, `#vitest`, `#test-harness`. Load-bearing:
  - `2026-04-12 #vitest #frontend` — `@testing-library/react` auto-cleanup needs `test.globals: true` in `vitest.config`.
  - `2026-04-12 #vitest #frontend` — jsdom lacks `HTMLDialogElement.showModal()` (not relevant here, but sets the bar for "is this primitive jsdom-safe"). The matching jsdom gap for this story is `scrollIntoView({behavior:'smooth'})` + `scrollend` — both are no-ops; tests assert state + spy calls, NOT visual scroll position (story §3.2 jsdom note already aligns).
  - `2026-04-11 #vitest #test-harness` — Vitest 2.x AST-hoists `vi.mock` above local consts; wrap spies in `vi.hoisted(() => ({ spy: vi.fn() }))` if mocking `useScrollspy`.
- **025-02..04:** `#frontend`, `#tailwind`, `#vitest`. Load-bearing:
  - `2026-04-11 #ui` — Tailwind 4 `@theme` declares custom tokens only; slate/zinc/red/blue ship by default. Re-skin uses ONLY existing tokens (per epic `architecture_rules`). No new `@theme`.
  - `2026-04-11 #frontend #recipe` — All frontend fetches via `useQuery`/`useMutation` → typed functions in `frontend/src/lib/api.ts`. Never raw `fetch()`. Re-skins reuse existing hooks unchanged.
  - `2026-04-24 #vitest #test-harness` — Button label flips on `isPending`: render with `isPending: false`, click, `rerender` with `isPending: true` (precaution for KeySection Rotate / Channels Bind).
- **025-05:** `#fastapi`, `#auth`, `#supabase`, `#pytest`. Load-bearing:
  - `2026-04-25 #test-harness #fastapi #lifespan` — `with TestClient(app) as client:` deadlocks under `pytest-asyncio` auto. Use `TestClient(app, raise_server_exceptions=False)` without context manager (existing pattern in `test_workspaces_team_member_access.py`).
  - `2026-04-13 #fastapi` — Never `from __future__ import annotations` in FastAPI routes; `workspaces.py` is verified clean of this — do not add.
  - `2026-04-13 #schema #test-harness` — Hermetic Supabase mocks don't validate column names; verify `teemo_slack_team_members(slack_team_id, user_id, role)` columns against live migration before coding mocks.
  - `2026-04-24 #schema #auth` — When a fix narrows write access, re-check downstream invariants. Adding owner gate on DELETE — confirm no existing test asserts non-creator member can DELETE (none expected; documented in §7 open question).
- **025-06:** `#frontend`, `#cutover`. Load-bearing:
  - `2026-04-12 #fastapi #frontend` — Starlette `StaticFiles(html=True)` is NOT an SPA fallback (no impact here, but reminds that route changes can hit catch-all). Cutover is JSX-only.
  - `2026-04-11 #frontend #ci` — After adding `routes/*.tsx`, run `vite build` first (regenerates `routeTree.gen.ts`). 025-06 doesn't add a route, but the route file shrinks dramatically — verify `routeTree.gen.ts` doesn't reference deleted exports.
- **CR-001:** `#frontend`, `#vitest`. Reuses 025-02's re-skinned ChannelSection. Trivial.
- **BUG-001:** `#frontend`. **PRE-FLIGHT BLOCKER — see §7 OQ-1.** Only proceed if AppNav truly needs changes.
- **Sprint-wide:** `2026-04-24 #reporting #hook` — `.cleargate/sprint-runs/.active` already holds `SPRINT-16` (verified). Token-ledger hook still backlogged — fourth sprint without rows; Reporter flags.

---

## 1. Red-zone & shared surfaces

| File | Touched by | Sequencing rule |
|---|---|---|
| `frontend/src/components/workspace/moduleRegistry.ts` (NEW in 025-01) | **025-01** creates · **025-02 / 03 / 04 / 05** each append entries | Strict order 01 → 02 → 03 → 04 → 05; per-story append blocks (§3 specifies which group each adds) prevent merge conflicts. |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | **025-01** mounts shell + drops SetupStepper guard · **025-02 / 03 / 04 / 05** each delete inline section bodies · **025-06** deletes legacy stacked block + SetupStepper component file | **Strict merge order 01 → 02 → 03 → 04 → 05 → 06.** Each commit's diff against the file is enumerated per story in §3. |
| `frontend/src/components/workspace/ChannelSection.tsx` | **025-02** re-skins to divider list · **CR-001** adds search input + count badge above list | **Hard sequence: CR-001 lands AFTER 025-02 merges.** CR-001 inserts search above the divider list 025-02 introduces. |
| `frontend/src/components/layout/AppNav.tsx` | **BUG-001 only** | Independent file space. **CONSTRAINT: do not change nav `h-14` height** — sticky tab bar in 025-01 uses `top-14` and reads AppNav height implicitly. **Pre-flight: confirm BUG-001 is still needed** — see §7 OQ-1. |
| `frontend/src/lib/api.ts` | **025-05 only** (adds `is_owner?: boolean` to `Workspace` interface at line 234) | Single-file surface. |
| `backend/app/api/routes/workspaces.py` | **025-05 only** (adds owner-role gate + `is_owner` field) | Single-file surface. **Premise correction: see §7 OQ-2.** |

No surface is touched by 4+ items where strict ordering can't resolve it cleanly.

---

## 2. Sequencing recap (mirror of SPRINT-16.md §3)

1. **STORY-025-01 first** — foundation. Creates 6 new files + drops SetupStepper guard. Unblocks every other EPIC-025 story.
2. **STORY-025-02..05 + BUG-001 in parallel after 025-01** — different file spaces (except `moduleRegistry.ts` and route, both append/delete-only with deterministic merge resolution).
3. **CR-001 lands after 025-02 merges** — same file, adding above the new divider list.
4. **STORY-025-06 last** — strictly blocked by 025-02..05 + CR-001. Deletes legacy stacked JSX from route, deletes `SetupStepper.tsx`, adds mobile responsive treatment.

**Per-item branches + commit prefixes (per SPRINT-16.md §4):**
- `story/STORY-025-01-shell-foundation` → `feat(epic-025): STORY-025-01 shell foundation`
- `story/STORY-025-02-connections-migration` → `feat(epic-025): STORY-025-02 connections migration`
- `story/STORY-025-03-knowledge-migration` → `feat(epic-025): STORY-025-03 knowledge migration`
- `story/STORY-025-04-behavior-migration` → `feat(epic-025): STORY-025-04 behavior migration`
- `story/STORY-025-05-workspace-owner-gate` → `feat(epic-025): STORY-025-05 workspace owner gate`
- `story/STORY-025-06-mobile-cutover` → `feat(epic-025): STORY-025-06 mobile + cutover`
- `cr/CR-001-channel-picker-search` → `feat(epic-005): CR-001 channel picker search`
- `bug/BUG-001-nav-aesthetics` → `fix(epic-023): BUG-001 nav glassmorphism` (only if §7 OQ-1 resolves "still needed")

---

## 3. Per-story blueprints

### STORY-025-01 — Shell foundation (frontend, L2, P1, blocks 02..06)

**Pre-flight (Developer must run at branch tip):**
```bash
# Confirm route file still at ~1235 LOC and has not been pre-modified
wc -l frontend/src/routes/app.teams.\$teamId.\$workspaceId.tsx
# EXPECT: 1235 lines (verified 2026-04-25)

# Confirm no scaffolding files already exist
ls frontend/src/components/workspace/WorkspaceShell.tsx 2>&1 | grep -q "No such" && echo "OK: clean slate"
```

**Files (exhaustive):**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/useScrollspy.ts` | NEW | Exports `HEADER_OFFSET = 140` (number), `SCROLLSPY_THRESHOLD = 200` (number), and default-exports `useScrollspy(groupAnchorIds: string[]): { activeGroupId: string; setProgrammaticScrolling: (v: boolean) => void }`. Hook signature in story §3.2; resolver-per-frame logic mandatory (programmatic gate → end-of-page guard → topmost-≤-200). |
| `frontend/src/components/workspace/moduleRegistry.ts` | NEW | Typed registry: `export type ModuleStatus = 'ok' \| 'partial' \| 'empty' \| 'error' \| 'neutral'; export type ModuleGroup = 'connections' \| 'knowledge' \| 'behavior' \| 'workspace'; export interface ModuleEntry { id: string; group: ModuleGroup; label: string; icon: LucideIcon; summary?: string; statusResolver: (data: WorkspaceData) => ModuleStatus }`. Initial `MODULE_REGISTRY: ModuleEntry[] = []` empty array — entries appended by 025-02..05. Also export `GROUP_ORDER: ModuleGroup[] = ['connections', 'knowledge', 'behavior', 'workspace']` and `GROUP_LABELS: Record<ModuleGroup, string>`. **`WorkspaceData` is a NEW type aggregating all data the resolvers need** — see §5 for exact shape. |
| `frontend/src/components/workspace/ModuleSection.tsx` | NEW | Props: `{ id: string; title: string; caption?: string; action?: ReactNode; children: ReactNode }`. Renders `<section id={\`tm-${id}\`} style={{scrollMarginTop: HEADER_OFFSET}} className="space-y-4">` with header (`<h2 className="text-base font-semibold text-slate-900">` + caption + optional action slot) followed by `<div className="rounded-lg border border-slate-200 bg-white">{children}</div>`. Imports `HEADER_OFFSET` from `./useScrollspy`. |
| `frontend/src/components/workspace/StatusStrip.tsx` | NEW | Props: `{ cells: StatusCell[] }` where `StatusCell = { kicker: string; value: string; caption?: string }`. Renders `<div className="grid grid-cols-2 md:grid-cols-4 gap-3">{cells.map(...)}</div>`. **Always 4 cells (Setup dropped per epic §6 Q4)**. Cells are informational, NOT clickable — render as `<div>` not `<button>`. Per-cell markup: `<div className="rounded-lg border border-slate-200 bg-white p-4"><div className="text-[11px] font-medium uppercase tracking-wider text-slate-500">{kicker}</div><div className="text-sm font-semibold text-slate-900">{value}</div>{caption && <div className="text-xs text-slate-500">{caption}</div>}</div>`. |
| `frontend/src/components/workspace/StickyTabBar.tsx` | NEW | Props: `{ groups: { id: ModuleGroup; label: string; okCount: number; total: number }[]; activeGroupId: string; onTabClick: (groupId: ModuleGroup) => void }`. Renders `<div className="sticky top-14 z-20 -mx-4 px-4 py-2 bg-slate-50/90 backdrop-blur-sm border-b border-slate-200">` with a `<div className="flex gap-2">` of buttons. Each button: `bg-white border border-slate-200 shadow-sm` when active else `bg-transparent text-slate-600 hover:bg-white/50`. Pill: `<span className="ml-2 text-xs text-slate-500">{okCount} / {total}</span>`. (Mobile overflow-x-auto + auto-scroll-into-view in 025-06.) |
| `frontend/src/components/workspace/WorkspaceShell.tsx` | NEW | Props: `{ workspace: Workspace; teamId: string }`. Internally fetches the rest via existing hooks (`useDriveStatusQuery`, `useKeyQuery`, `useKnowledgeQuery`, `useChannelBindingsQuery`, `useSkillsQuery`, `useAutomationsQuery`) and assembles `WorkspaceData`. Composes: header (breadcrumb + h1) → `<StatusStrip />` → `<StickyTabBar />` → groups loop rendering `<ModuleSection />` placeholders (real bodies filled by 025-02..05). `useScrollspy` mounted; `useLayoutEffect` reads `window.location.hash` cold-load and calls `el.scrollIntoView()` after one `requestAnimationFrame`. Tab-click handler per story §3.2 verbatim (sets `isProgrammaticScroll`, `scrollend` listener with 600ms `setTimeout` fallback gated by `'onscrollend' in window`). |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | **Lines 1097-1105:** delete the `if (!isSetupComplete && !wizardSkipped) return <SetupStepper .../>` block entirely. Also delete `wizardSkipped` state (line 1008) + `isSetupComplete` const (line 1071) since they become dead. **Lines 1107-1232 stay AS-IS in this story** (legacy stacked JSX still renders). The route renders `<WorkspaceShell workspace={workspace} teamId={teamId} />` ABOVE the legacy `<div className="min-h-screen ...">` block — both render simultaneously during 025-01..05. (025-06 deletes the legacy block.) Modal mounts (lines 1205-1220) stay. SetupStepper IMPORT at line 46 stays as dead-code (deletion in 025-06). |

**OFF-LIMITS:**
- `frontend/src/components/workspace/SetupStepper.tsx` — file stays in tree (deletion in 025-06).
- All inline section function definitions (lines 197-906) — extracted in 025-02..05.
- `frontend/src/app.css` — token reference only.

**Vitest scenarios (9, one per Gherkin in story §2.1):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `StatusStrip renders 4 cells at md+` | `render(<StatusStrip cells={[Workspace, Slack, Provider, Knowledge]} />)`; `screen.getAllByRole('article').length === 4` (or query by kicker text); no cell text matches `/setup/i`. |
| 2 | `StatusStrip collapses to 2 columns below md` | Inspect class list contains `grid-cols-2` + `md:grid-cols-4` (visual breakpoint not testable in jsdom; assert class names). |
| 3 | `Sticky tab bar activates on scroll past threshold` | Mount shell with mocked `useScrollspy` returning `activeGroupId='knowledge'` after a state update; assert Knowledge tab gets `bg-white border-slate-200 shadow-sm` class subset. |
| 4 | `Tab click smooth-scrolls with HEADER_OFFSET (140px)` | Spy `Element.prototype.scrollIntoView`; click Behavior tab; expect spy called with `{behavior:'smooth', block:'start'}`; expect `window.location.hash === '#tm-persona'`; expect the persona section's inline `style.scrollMarginTop === '140px'`. |
| 5 | `No active-tab flicker during programmatic scroll` | Spy `setProgrammaticScrolling` ref. After click, fire a `scroll` event with the OTHER group's section in viewport; assert `activeGroupId` does NOT change. Then dispatch `scrollend` (or advance fake timers 600ms); assert `setProgrammaticScrolling(false)` was called. |
| 6 | `End-of-page activates last group` | Mock `document.documentElement.scrollHeight`, `window.innerHeight`, `window.scrollY` so `scrollY + innerHeight >= scrollHeight - 8`; fire scroll; assert `activeGroupId === lastGroupId` even when no section's top crosses 200px. |
| 7 | `Cold-load deep link lands at HEADER_OFFSET` | Set `window.location.hash = '#tm-files'` BEFORE `render()`; spy `scrollIntoView`; advance one `requestAnimationFrame`; assert spy called on element with `id="tm-files"`; assert resolver picks `knowledge` group. |
| 8 | `Shell renders without SetupStepper guard` | Render route component with `bot_persona = ''`, `has_key=false`, `driveConnected=false`; assert `<WorkspaceShell />` mounts AND no element with text `/get started|setup wizard/i` is rendered. |
| 9 | `Status resolver dispatch` | Provide registry with two stub entries whose resolvers are `vi.fn()`; render shell; assert each fn called once with the workspace data object; assert returned status is forwarded to StatusStrip caption + tab pill. |

**Reuse (no duplication):**
- Existing `Card.tsx`, `Button.tsx`, `Badge.tsx` from `frontend/src/components/ui/*`.
- Existing `useWorkspaceQuery`, `useDriveStatusQuery`, `useKeyQuery`, `useKnowledgeQuery`, `useChannelBindingsQuery`, `useSkillsQuery`, `useAutomationsQuery` from `frontend/src/hooks/*`. **Do NOT add new hooks in 025-01.**
- `lucide-react` icons (per epic `architecture_rules`).

**Risks beyond sprint plan §5:**
- `useScrollspy` Vitest tests need fake timers for the 600ms `scrollend` fallback — use `vi.useFakeTimers()` + `vi.advanceTimersByTime(600)`. Forgetting this leaves scenario #5 hanging.
- `WorkspaceData` type must be exported from `moduleRegistry.ts` (or a sibling `types.ts`) so 025-02..05 can extend the resolvers without each story redeclaring the shape. See §5.

**Commit message:** `feat(epic-025): STORY-025-01 shell foundation — sticky tabs + scrollspy + status strip`

---

### STORY-025-02 — Connections migration (frontend, L2, P1, blocks CR-001)

**Pre-flight:**
```bash
# Confirm 025-01 landed
ls frontend/src/components/workspace/{ModuleSection,WorkspaceShell,moduleRegistry,useScrollspy,StatusStrip,StickyTabBar}.tsx 2>&1 | grep -v "No such" | wc -l
# EXPECT: 6
```

**Files:**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/SlackSection.tsx` | NEW | Pure presentational. Props: `{ workspace: Workspace; teamId: string }`. Renders the avatar tile + workspace name + mono caption + `<Badge variant="success">Installed</Badge>`. **NO Reinstall button** (epic §6 Q1). **Caption (resolved OQ-3 = C):** read both `workspace.slack_team_id` and `workspace.slack_domain` from `useWorkspaceQuery` (fields added by STORY-025-05). Render: ``${workspace.slack_team_id}${workspace.slack_domain ? ` · ${workspace.slack_domain}` : ''}``. Defensive when `slack_domain` is null/undefined → caption shows team_id only. |
| `frontend/src/components/workspace/DriveSection.tsx` | NEW (extracted from route lines 197-252) | Copy verbatim, swap inner `<Card>` for `<div className="p-4">` (the `<ModuleSection>` parent already provides the bordered card). Preserve `useDriveStatusQuery` + `useDisconnectDriveMutation` calls. Caption: "Read-only access · scoped to selected files" (handoff README §"Module bodies"). |
| `frontend/src/components/workspace/KeySection.tsx` | MODIFY | Re-skin to: 3-button segmented control (Google / OpenAI / Anthropic) + `<div className="rounded-md bg-slate-50 p-3 font-mono text-xs">{maskedKey}</div>` + Rotate button. Caption: `"Encrypted with AES-256-GCM. Last validated {N} {unit} ago."` Existing key persistence + validation hooks unchanged. **Build segmented control inline with Tailwind classes** (per story §3.2 — no new primitive). |
| `frontend/src/components/workspace/ChannelSection.tsx` | MODIFY | Re-skin to divider list: `<ul className="divide-y divide-slate-100">` of rows. Each row: `<li className="flex items-center justify-between px-4 py-3"><span className="font-mono text-sm">#{channel.name}</span><div>{bound && <Badge>Bound</Badge>}<Button variant={bound ? 'secondary' : 'primary'}>{bound ? 'Unbind' : 'Bind'}</Button></div></li>`. Picker overlay (lines 230-280) preserved verbatim — CR-001 will add a search input above the existing `allChannels.map` at line 239. **Existing `useChannelBindingsQuery`, `useBindChannelMutation`, `useUnbindChannelMutation` unchanged.** Existing `__tests__/ChannelSection.test.tsx` must pass without modification (or with class-name-only updates). |
| `frontend/src/components/workspace/moduleRegistry.ts` | MODIFY | Append 4 entries: `slack`, `drive`, `key`, `channels` — all `group: 'connections'`. Status resolvers per story §1.2 (Slack `ok` if installed; Drive `ok` if connected else `empty`; Key `ok` if `has_key` else `empty`; Channels `ok` if any bound else `empty`). |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | Delete inline `function DriveSection(...)` block (lines 197-252). Delete the `<DriveSection workspaceId={workspaceId} />` JSX usage at line 1143 (still rendered by legacy block; legacy block stays intact except for this one line — replace with `null` or remove). The new shell renders DriveSection via the registry. Other inline sections stay. |

**OFF-LIMITS:**
- `KnowledgeList`, `PickerSection`, `PersonaSection`, `SkillsSection`, `DeleteWorkspaceSection`, `TruncationToast` (lines 301-1000+) — owned by 025-03..05.
- The picker overlay markup in `ChannelSection.tsx` lines 230-280 — CR-001 will modify this independently after 025-02 merges.

**Vitest scenarios (5):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Slack module renders info-only with team_id + domain` | Render `<SlackSection workspace={{...mockWorkspace, slack_team_id: 'T1', slack_domain: 'acme.slack.com'}} teamId="T1" />`; assert avatar tile present, workspace name visible, mono caption text matches "T1 · acme.slack.com", `Badge` text "Installed" present, **NO button with text /reinstall/i**. |
| 2 | `Slack module degrades to team_id when domain absent` | Render with `slack_domain: null`; assert mono caption text matches "T1" exactly (no separator, no domain). |
| 3 | `Drive disconnect preserves existing behavior` | Render `<DriveSection workspaceId="w1" />` with mock `useDriveStatusQuery` returning `{connected:true, email:'a@b.com'}`; click Disconnect; assert `useDisconnectDriveMutation.mutate` invoked once. |
| 4 | `Provider segmented control persists selection` | Render `<KeySection workspaceId="w1" />` with mock returning `{has_key:true, provider:'openai'}`; assert OpenAI segment has active class subset; click Google; assert internal state updates and existing save mutation can fire. |
| 5 | `Channels divider list renders bound badge` | Render `<ChannelSection />` with 2 bound + 1 unbound channel; assert 2 rows have `Badge` text "Bound" + button text "Unbind"; 1 row has button text "Bind"; assert no `Card` border between rows (use `container.querySelectorAll('.divide-y > li').length === 3`). |

**Reuse:** `Button`, `Badge`, `Card` from `ui/*` unchanged. `useDriveStatusQuery`, `useDisconnectDriveMutation`, `useKeyQuery`, all channel hooks unchanged. **Avatar tile pattern (40×40 slate-100 + lucide icon at 20px slate-500) is reused by SlackSection + DriveSection — extract a `<ModuleAvatarTile icon={...} />` helper inside one of these files and import from the other** to prevent CSS drift in 025-03 (KnowledgeList row icon, different size) and 025-04 (Automation empty-state tile, same size).

**Risks beyond sprint plan §5:**
- The masked-key string in KeySection (story §1.2: `sk-proj-•••• G7vT`) — verify the existing `useKeyQuery` already returns a masked form, or compute mask client-side from `key_hint`. Don't store the raw key.
- Channels divider list: existing `__tests__/ChannelSection.test.tsx` may assert per-row `Card` markup. Inspect before re-skin; if so, update class assertions.

**Commit message:** `feat(epic-025): STORY-025-02 connections migration — Slack/Drive/Key/Channels divider list`

---

### STORY-025-03 — Knowledge migration (frontend, L1, P1)

**Pre-flight:**
```bash
# Confirm 025-01 landed
ls frontend/src/components/workspace/ModuleSection.tsx >/dev/null && echo OK
# Confirm PickerSection + KnowledgeList still inline at expected lines
grep -nE "^function (PickerSection|KnowledgeList|TruncationToast)" frontend/src/routes/app.teams.\$teamId.\$workspaceId.tsx
# EXPECT (verified 2026-04-25):
#   301: function PickerSection
#   572: function KnowledgeList
#   693: function TruncationToast
```

**Files:**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/FilesSection.tsx` | NEW | Composes PickerSection + KnowledgeList behavior. Header strip: `<div className="flex items-center justify-between gap-3 border-b border-slate-200 px-4 py-3">{N} of {M} files indexed | <Button>Add file</Button><Button variant="secondary">Upload</Button></div>`. Below header: TruncationToast banner (when active), then `<ul className="divide-y divide-slate-100">` of file rows. Each row uses `group` Tailwind class so Remove button uses `opacity-0 group-hover:opacity-100 transition-opacity duration-150`. Props: `{ workspaceId: string; driveConnected: boolean; hasKey: boolean }`. Internally uses existing `useKnowledgeQuery`, `useAddKnowledgeMutation`, `useUploadKnowledgeMutation`, `useRemoveKnowledgeMutation`, `useReindexKnowledgeMutation` — all imported from `../../hooks/useKnowledge`. |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | Delete inline `function PickerSection` (lines 301-570 — re-grep at branch tip), `function KnowledgeList` (lines 572-691), `function TruncationToast` (lines 693-737). **TruncationToast moves into FilesSection.tsx** (story §1.2 explicit). Remove `<PickerSection ...>` + `<KnowledgeList ...>` JSX usages from legacy block (lines 1146-1167) — replace with `null` or remove entirely; legacy block retains `<PersonaSection />`, `<SkillsSection />`, `<AutomationsSection />`, `<ChannelSection />`, `<DeleteWorkspaceSection />` for 025-04/05 to handle. |
| `frontend/src/components/workspace/moduleRegistry.ts` | MODIFY | Append 1 entry: `files` — `group: 'knowledge'`. Status resolver: `ok` if `files.length >= 15`, `partial` if `1 <= files.length < 15`, `empty` if `0`. (Match handoff "Files: ok if 1+ indexed, partial if <15".) |

**OFF-LIMITS:**
- `frontend/src/hooks/useKnowledge.ts` — no changes.
- `frontend/src/lib/api.ts` — no changes.
- All non-Knowledge inline section bodies in route file.

**Vitest scenarios (4):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Header strip renders count + actions` | Mock `useKnowledgeQuery` returning 12 files; render `<FilesSection ...>`; assert text `/12 of 100 files indexed/`, button text `/add file/i`, button text `/upload/i`. |
| 2 | `Divider list renders rows without per-row cards` | 3 files mocked; assert `container.querySelectorAll('.divide-y > li').length === 3`; assert no `<div class*="border">` wraps each row (only the outer `<ModuleSection>` card). |
| 3 | `Remove button hover-reveals` | 1 file row; assert Remove button has class `opacity-0`; trigger `fireEvent.mouseOver` on row; assert class includes `group-hover:opacity-100` (visual reveal not testable in jsdom — assert class-list presence). |
| 4 | `Existing add/upload behavior preserved` | Click Add file → assert existing picker open path runs (mock `loadGapiScript` + Google API). Click Upload + simulate file selection → assert `useUploadKnowledgeMutation.mutate` invoked. |

**Reuse:** All existing `useKnowledge*` hooks unchanged. `Button`, `Badge` from `ui/*`. The divider-list + hover-reveal pattern is the prototype for 025-04 SkillsSection — keep markup copy-pasteable.

**Risks beyond sprint plan §5:**
- File cap is **100**, not 15 (per S-15 R-CAP DRIFT lesson: `database/migrations/012_increase_document_cap.sql`). The "15" in the status resolver is the `partial`/`ok` threshold, NOT the cap. The header strip count `{N} of {M}` uses **M = 100** (the actual cap), matching what PickerSection currently shows.
- TruncationToast move: the route file currently passes `truncationWarning` state into TruncationToast at line 1135-1140. FilesSection must own that state internally (lift from route into FilesSection) so the route doesn't need a `truncationWarning` prop. Verify the indexing banner at line 1156-1160 also moves into FilesSection.

**Commit message:** `feat(epic-025): STORY-025-03 knowledge migration — Files header strip + divider list`

---

### STORY-025-04 — Behavior migration (frontend, L1, P1)

**Pre-flight:**
```bash
grep -nE "^function (PersonaSection|SkillsSection)" frontend/src/routes/app.teams.\$teamId.\$workspaceId.tsx
# EXPECT (verified 2026-04-25):
#   739: function PersonaSection
#   831: function SkillsSection
```

**Files:**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/PersonaSection.tsx` | NEW (extracted from route lines 739-829) | Copy function body verbatim. Replace inner `<Card>` + h2 with bare content (the `<ModuleSection>` parent provides card + header). Preserve `useUpdateWorkspaceMutation`, character counter, save/error toast, the existing textarea + Save button. **NO voice presets** (epic §6 Q2). |
| `frontend/src/components/workspace/SkillsSection.tsx` | NEW (extracted from route lines 831-906) | Copy function body verbatim. Re-skin to divider list (per row: lucide `Sparkles` icon + `<span className="font-mono text-xs bg-rose-50 text-rose-500 px-2 py-0.5 rounded">/teemo {name}</span>` + caption `{summary}` slate-500 12px). **NO Edit button** (ADR-023). Preserve `useSkillsQuery`. |
| `frontend/src/components/workspace/AutomationsSection.tsx` | MODIFY | Locate the empty-state JSX branch (grep for `'No automations'` or `automations.length === 0`). Replace markup with: `<div className="flex flex-col items-center gap-3 py-8"><div className="h-10 w-10 rounded-md bg-slate-100 flex items-center justify-center"><Zap className="h-5 w-5 text-slate-500" /></div><h3 className="text-base font-semibold">No automations yet</h3><p className="text-sm text-slate-500">Trigger Tee-Mo on a schedule, on a Slack event, or from a webhook.</p><Button variant="secondary" onClick={onAddClick}>Create automation</Button></div>`. **Populated state branch UNTOUCHED** — existing tests must pass. |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | Delete inline `function PersonaSection` (lines 739-829) and `function SkillsSection` (lines 831-906). Remove `<PersonaSection workspace={workspace} />` (line 1170) + `<SkillsSection workspaceId={workspaceId} />` (line 1173) from legacy block. AutomationsSection stays in tree (it's already a separate file); its JSX usage at line 1176 stays in legacy block until 025-06. |
| `frontend/src/components/workspace/moduleRegistry.ts` | MODIFY | Append 3 entries: `persona`, `skills`, `automations` — all `group: 'behavior'`. Status resolvers: Persona `ok` if `bot_persona` non-empty else `empty` (epic §6 Q5: NEVER `partial`); Skills `ok` if `skills.length > 0` else `empty`; Automations `ok` if `automations.length > 0` else `empty`. |

**OFF-LIMITS:**
- AutomationsSection populated-state JSX (epic §1.3, story §1.3).
- SkillsSection backend / hook changes (read-only per ADR-023).

**Vitest scenarios (5):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Persona behavior preserved` | Render `<PersonaSection workspace={mock}>`; type new persona; click Save; assert `useUpdateWorkspaceMutation.mutate` invoked with `{bot_persona: 'new text'}`; assert "Saved successfully" pill renders on success. |
| 2 | `Skills divider list renders without Edit` | Mock `useSkillsQuery` with 2 skills; assert 2 rows separated by dividers; assert each row has Sparkles icon + mono `/teemo {name}` chip + summary caption; **assert NO button with text /edit/i**. |
| 3 | `Automation empty state matches handoff` | Mock `useAutomationsQuery` returning `[]`; assert slate-100 zap tile present, headline "No automations yet", caption text matches, button text "Create automation". |
| 4 | `Automation populated state preserved` | Mock `useAutomationsQuery` returning 2 automations; assert existing populated list rendering matches pre-redesign snapshot (run existing AutomationsSection test as regression). |
| 5 | `Persona status reflects empty vs filled` | Call `personaResolver({bot_persona: ''})` → assert `'empty'`. Call `personaResolver({bot_persona: 'foo'})` → assert `'ok'`. Render `<PersonaSection workspace={{bot_persona:''}}>` → assert textarea present with placeholder. |

**Reuse:** `useUpdateWorkspaceMutation`, `useSkillsQuery`, `useAutomationsQuery` unchanged. The divider-list pattern from 025-03 FilesSection — copy markup. Lucide `Sparkles`, `Zap`, `UserRound` icons.

**Risks beyond sprint plan §5:**
- PersonaSection's character counter + save toast may use local state that doesn't survive verbatim extraction. Re-render existing PersonaSection unit tests after extraction (if they exist — they may not; story §4.1 says the resolver test is new).
- AutomationsSection empty-state has an existing test in `__tests__/AutomationsSection.test.tsx`. Re-run before commit; update snapshot if needed.

**Commit message:** `feat(epic-025): STORY-025-04 behavior migration — Persona/Skills/Automations`

---

### STORY-025-05 — Workspace + owner gate (frontend + backend, L1, P1)

**Pre-flight (resolved at kickoff, OQ-2 = C):**
```bash
# Confirm current DELETE handler filters by .eq("user_id", user_id) — creator-only, 404 leak guard.
grep -nE "@router.delete|def delete_workspace|.eq..user_id..user_id." backend/app/api/routes/workspaces.py | head -10
# EXPECT: line 411 @router.delete; line 415 def delete_workspace; line 443 .eq("user_id", user_id)
# Confirm teemo_slack_team_members schema
grep -rE "teemo_slack_team_members" database/migrations/ | head -5
```

**Resolved gate semantics:** creator-OR-team-owner OR. Either predicate is enough. Error contract: 204 success, 403 when caller is team-member-but-neither, 404 when caller is non-member or workspace missing (preserves ADR-024 existence-leak guard).

**Files:**

| File | Action | Notes |
|---|---|---|
| `backend/app/api/routes/workspaces.py` | MODIFY | (1) Add `async def is_team_owner(team_id: str, user_id: str) -> bool` predicate helper near `assert_team_member` at line 53. Queries `teemo_slack_team_members` for `(slack_team_id, user_id, role='owner')` and returns bool. (2) Rewrite `delete_workspace` per STORY-025-05 §3.2 code block: SELECT row by id only (404 if missing) → check `is_creator OR is_team_owner` (succeed) → else probe membership: 404 if not member, 403 if member-but-neither → DELETE by id. (3) Rewrite `get_workspace` to surface `is_owner: bool` AND `slack_domain: str \| None`. `is_owner` from `teemo_slack_team_members.role==owner` for caller. `slack_domain` from `teemo_slack_teams.domain` joined on `slack_team_id` (null defensively). Update `_to_response` (line 93). **List endpoint `list_workspaces` (line 130) does NOT include either field** — detail-only contract. |
| `backend/app/models/workspace.py` | MODIFY | Add `is_owner: bool = False` AND `slack_domain: str \| None = None` to `WorkspaceResponse`. One-line comment: `# Detail-only fields (GET /workspaces/{id}); never on list endpoints — see STORY-025-05.` |
| `frontend/src/lib/api.ts` | MODIFY | Line 234 `interface Workspace`: add `is_owner?: boolean` AND `slack_domain?: string \| null`. Optional because list endpoint omits. STORY-025-02 SlackSection consumes `slack_domain` (read-only). |
| `frontend/src/components/workspace/DangerZoneSection.tsx` | NEW (extracted from route lines 908-1005) | Copy `function DeleteWorkspaceSection` verbatim. Re-skin to single-row layout: `<div className="flex items-center justify-between p-4"><div><h3 className="text-base font-semibold">Danger zone</h3><p className="text-sm text-slate-500">Delete or transfer workspace</p></div><Button variant="danger" onClick={onDeleteClick}>Delete</Button></div>`. Preserve existing div-based confirmation dialog. |
| `frontend/src/components/workspace/moduleRegistry.ts` | MODIFY | Append 1 entry: `danger-zone` — `group: 'workspace'`. Status resolver: always returns `'neutral'` (action-only). |
| `frontend/src/components/workspace/WorkspaceShell.tsx` | MODIFY | Filter registry: `const visibleEntries = MODULE_REGISTRY.filter(e => e.group !== 'workspace' || workspace.is_owner === true);`. Pass filtered list to StickyTabBar AND ModuleSection loop. If `workspace` group has zero visible modules, suppress the tab entirely. |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | Delete inline `function DeleteWorkspaceSection` (lines 908-1005). Remove `<DeleteWorkspaceSection ...>` JSX (line 1226-1230) from legacy block. |

**OFF-LIMITS:**
- `assert_team_member` helper (lines 53-85) — do NOT modify, do NOT broaden, do NOT touch. BUG-002 regression risk.
- `list_workspaces` (line 130), `create_workspace` (line ~190), `list_slack_channels` (any) — these are the BUG-002-relaxed endpoints; keep them as-is.
- All other DELETE endpoints in `workspaces.py` (none currently exist for sub-resources, but verify).

**Pytest scenarios (5 — covers the OR matrix + GET surface; story §4.1 min is 5):**

| # | Test name (suggested) | Asserts |
|---|---|---|
| 1 | `test_delete_workspace_owner_not_creator_succeeds` | Mock workspace SELECT returns row with `user_id != caller`; mock `is_team_owner` returns True. `client.delete(...)` → 204. |
| 2 | `test_delete_workspace_creator_not_owner_succeeds` | Mock SELECT returns row with `user_id == caller`; `is_team_owner` returns False. `client.delete(...)` → 204. (Creator self-service preserved.) |
| 3 | `test_delete_workspace_member_neither_returns_403` | Mock SELECT returns row with different `user_id`; `is_team_owner` False; membership probe returns `[{role:'member'}]`. `client.delete(...)` → 403, body `{"detail": "Only the workspace creator or a team owner can delete this workspace."}`. |
| 4 | `test_delete_workspace_non_member_returns_404` | SELECT returns row; `is_team_owner` False; membership probe empty. `client.delete(...)` → 404, body `{"detail": "Workspace not found."}`. (ADR-024 existence-leak guard.) |
| 5 | `test_get_workspace_surfaces_is_owner_and_slack_domain` | Mock joined query returns role `'owner'` + domain `'acme.slack.com'`. `client.get(...)` → 200, JSON includes `"is_owner": true` AND `"slack_domain": "acme.slack.com"`. |

**BUG-002 regression check (pytest, additional safeguard — must pass without modification):**
- `backend/tests/test_workspaces_team_member_access.py` (assumed file based on flashcard naming) — re-run; assert member can still LIST + CREATE workspaces. If this file doesn't exist, run the relevant existing `test_workspaces*` tests and verify member-access tests stay green.
- `client.get(f"/api/slack-teams/{team_id}/workspaces")` as a member → status 200 (UNCHANGED by 025-05).

**Vitest scenarios (2):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Workspace tab visible for owner` | Mock `useWorkspaceQuery` returning `{...workspace, is_owner: true}`; render shell; assert StickyTabBar contains tab with text "Workspace"; assert DangerZoneSection rendered. |
| 2 | `Workspace tab absent for member` | Mock returning `{...workspace, is_owner: false}`; render shell; assert StickyTabBar does NOT contain "Workspace" tab; assert no DangerZoneSection in DOM. |

**Risks beyond sprint plan §5:**
- The 403 vs 404 contract change for non-creator non-owner team members: today they get 404 (existence-leak guard per ADR-024). Tomorrow they get 403 with explicit "Only the workspace creator or a team owner can delete this workspace." Existence leak is bounded to inside the team membership boundary — caller is already a confirmed team member, so disclosing intra-team workspace existence is acceptable per the resolved spirit of ADR-024.
- `WorkspaceResponse` model gains TWO new fields (`is_owner`, `slack_domain`) — verify no existing callers break on extra-field validation. Pydantic permits extra by default; safe. Frontend type extension is `?:` so consumers needn't update.
- Frontend `useWorkspaceQuery` cache: when role flips out-of-band, user must reload (story §3.2 acknowledges). No code change for S-16.
- 025-02 SlackSection reads `slack_domain` from workspace GET. If 025-05 hasn't merged when 025-02 lands, the field is undefined and the caption falls back to team_id only. Forward-compatible. Recommended: merge 025-05 before 025-02 to light up the live caption immediately.

**Commit message:** `feat(epic-025): STORY-025-05 workspace owner gate — DELETE 403 + DangerZone hide`

---

### STORY-025-06 — Mobile + cutover (frontend, L2, P1, blocks DoD)

**Pre-flight:**
```bash
# Confirm 025-02..05 + CR-001 all merged
git log --oneline sprint/S-16 | head -10
# EXPECT: 7 commits (025-01 through 025-05 + CR-001 + BUG-001 if applicable)

# Confirm route file is bloated with both shell AND legacy
wc -l frontend/src/routes/app.teams.\$teamId.\$workspaceId.tsx
# EXPECT: ~700-900 (legacy shrunk by 02..05 extractions, but still has top-level legacy block)
```

**Files:**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/StickyTabBar.tsx` | MODIFY | Add mobile overflow: `<div className="flex gap-2 overflow-x-auto -mx-4 px-4 md:mx-0 md:px-0 md:overflow-visible [&::-webkit-scrollbar]:hidden">`. Tab buttons get `min-w-max md:min-w-0`. Add `tabRefs: useRef<Record<string, HTMLButtonElement \| null>>({})` plus `useEffect([activeGroupId])` that calls `tabRefs.current[activeGroupId]?.scrollIntoView({inline:'center', block:'nearest'})`. **`block:'nearest'` is critical** — prevents the scroll from also moving the page (story §3.2). |
| `frontend/src/components/workspace/WorkspaceShell.tsx` | MODIFY | Confirm container is `max-w-5xl mx-auto px-4 md:px-8 pt-6 pb-16` (handoff README §"Variation B"). |
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | MODIFY | **Delete the entire legacy `<div className="min-h-screen bg-slate-50 px-4 py-8">...</div>` block (lines 1108-1232 of pre-cutover state)** — leaves only `<WorkspaceShell />` + the modal mounts (`AddAutomationModal`, `DryRunModal`, `AutomationHistoryDrawer`). Modal mounts move INSIDE WorkspaceShell or stay as siblings — pick siblings to avoid prop drilling. **Delete unused imports** (`SetupStepper`, any helpers no longer needed). Final route file LOC target: **≤250** (sprint plan DoD). |
| `frontend/src/components/workspace/SetupStepper.tsx` | DELETE | Wizard retired (epic §6 Q3). |
| `frontend/src/components/workspace/__tests__/SetupStepper.test.tsx` | DELETE | Test file accompanies the deleted component. |
| route's existing test file (if present at `frontend/src/routes/__tests__/`) | MODIFY | Update assertions to expect `<WorkspaceShell />` mounted unconditionally; remove any SetupStepper-related assertions. |

**OFF-LIMITS:**
- AppNav.tsx — owned by BUG-001 (and likely already on main; see §7 OQ-1).
- All section component files — owned by their respective stories.
- Backend — zero changes in 025-06.

**Vitest scenarios (5):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Status strip 2-col on mobile` | Render `<StatusStrip cells={[...4]}>` with viewport hint; assert outer `<div>` class list contains `grid-cols-2 md:grid-cols-4`. |
| 2 | `Tab bar horizontally scrolls on mobile` | Render `<StickyTabBar groups={[4 groups]} ...>`; assert outer container class list contains `overflow-x-auto` + `md:overflow-visible`; assert tab buttons have `min-w-max md:min-w-0`. |
| 3 | `Mobile auto-scrolls active tab into bar viewport` | Spy `Element.prototype.scrollIntoView`; render with `activeGroupId='workspace'`; rerender with `activeGroupId='knowledge'`; assert spy called with `{inline:'center', block:'nearest'}` on the knowledge tab ref. |
| 4 | `Legacy stacked JSX removed (route renders only shell)` | Render route component; assert `<WorkspaceShell />` present; assert NO inline `function DriveSection` / `PickerSection` / `KnowledgeList` / `PersonaSection` / `SkillsSection` / `DeleteWorkspaceSection` exports/usages remain (use `screen.queryAllByTestId` against legacy markers if they had test-ids; otherwise read source via `__filename` is overkill — instead assert the rendered output contains the new shell components by data-testid). |
| 5 | `SetupStepper grep returns zero` | Vitest test runs `import.meta.glob('/frontend/src/**/*.{ts,tsx}', { as: 'raw' })` and asserts no matched file content includes the substring `SetupStepper`. (Or: run as a CI script, not a unit test — see Reporter recipe.) |

**Manual smoke (per sprint plan DoD):**
- 9 modules at viewport 375px + 1440px, one action per module.
- Deep links `#tm-files`, `#tm-skills`, `#tm-persona` cold-load.
- `grep -r "SetupStepper" frontend/src` returns zero matches.
- `wc -l frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` returns ≤250.

**Risks beyond sprint plan §5:**
- The `[&::-webkit-scrollbar]:hidden` Tailwind 4 arbitrary selector — verify Tailwind 4 supports the bracket syntax for pseudo-elements. If not, fall back to a `.scrollbar-hide` utility class declared in `app.css`.
- Active-tab auto-scroll fires on EVERY `activeGroupId` change including those triggered by scrollspy (not just tab clicks). If scrollspy fires rapidly during user-driven page scroll, the bar's horizontal scroll could fight visual stability. Mitigation: add a ref `lastUserActionWasTabClick` and only auto-scroll when that's true; OR debounce. (Recommend: ship as-is; smoke-test reveals if it's a real issue.)
- Modal mounts moving relative to legacy block deletion — verify `addAutomationOpen`, `dryRunOpen`, `historyAutomationId` state still lives at route scope (not lost during cleanup).

**Commit message:** `feat(epic-025): STORY-025-06 mobile + cutover — delete legacy stacked JSX + SetupStepper`

---

### CR-001 — Channel picker search (frontend, L1, P3, blocked-by 025-02)

**Pre-flight:**
```bash
# Confirm 025-02 merged and ChannelSection re-skinned to divider list
grep -n "divide-y\|<ul\|allChannels.map" frontend/src/components/workspace/ChannelSection.tsx | head -5
# Confirm picker overlay still at lines 230-280 (or near)
```

**Files:**

| File | Action | Notes |
|---|---|---|
| `frontend/src/components/workspace/ChannelSection.tsx` | MODIFY | Add `const [query, setQuery] = useState('')` at component scope. Above the `allChannels.map` (line 239 of pre-CR-001 state — re-grep at branch tip), insert `<input type="text" placeholder="Search channels…" value={query} onChange={e => setQuery(e.target.value)} className="w-full mb-2 px-3 py-1.5 border border-slate-200 rounded-md text-sm" />` and `<div className="text-xs text-slate-500 mb-1">{filtered.length} of {allChannels.length} channels</div>`. Compute `const filtered = allChannels.filter(ch => ch.name.toLowerCase().includes(query.trim().toLowerCase()));` and replace `allChannels.map(...)` with `filtered.map(...)`. Empty-filtered branch: when `allChannels.length > 0 && filtered.length === 0`, render `<p className="text-slate-500 text-sm">No channels match "{query}"</p>`. Existing `allChannels.length === 0` branch (line 235-236 — "No channels found in this Slack team.") preserved unchanged. |

**OFF-LIMITS:**
- Backend `channels.py` — no caching, no pagination, no debouncing.
- KnowledgeList, PickerSection, KeySection — out of scope.

**Vitest scenarios (3, per CR-001 §4):**

| # | Scenario | Asserts |
|---|---|---|
| 1 | `Renders search input + full list when query is empty` | Render with `allChannels=[3 channels]`; assert search input present; assert all 3 rows render; assert count badge "3 of 3 channels". |
| 2 | `Typing 'general' filters list and updates count badge` | Type "general" into search; assert only channels whose name (case-insensitive) contains "general" render; assert count badge updates. |
| 3 | `Substring with zero matches renders empty-state copy` | Type "zzznomatch"; assert `No channels match "zzznomatch"` rendered; assert original "No channels found in this Slack team." copy NOT rendered (because `allChannels.length > 0`). |

**Reuse:** No new primitives. Plain `<input>` + Tailwind classes.

**Risks beyond sprint plan §5:**
- Sequencing: if 025-02 hasn't merged, CR-001 will conflict on the picker overlay markup (which 025-02 may modify even though story §1.2 says picker is "preserved verbatim"). Hard-sequence per §2.

**Commit message:** `feat(epic-005): CR-001 channel picker search — name filter + count badge`

---

### BUG-001 — Nav glassmorphism (RESOLVED at pre-flight, no work)

OQ-1 resolved at kickoff = (a). All three implementation markers from BUG-001 §"Implementation Notes" already present in `frontend/src/components/layout/AppNav.tsx` on `main`:
- `bg-white/70 backdrop-blur-md` on the sticky nav container (line 55)
- Logo + "Workspaces" link clustered in single flex container (lines 57-74)
- `hover:text-brand-600 transition-all duration-200` on the Workspaces link (line 67)
- `<ul>/<li>` structure (lines 63-72)

BUG-001 frontmatter set to `status: Resolved`, `resolved_at: 2026-04-25`, `resolution: verified-shipped-no-commit`. Removed from active sprint scope. Constraint preserved for future stories: do NOT change `h-14` on nav container — StickyTabBar (025-01) uses `top-14`.

---

## 4. Cross-story risks (beyond SPRINT-16.md §5)

| Risk | Like. | Impact | Mitigation |
|---|---|---|---|
| `moduleRegistry.ts` merge conflicts when 025-02..05 land in parallel branches | Med | Low | Each story appends to a DIFFERENT location in the file (group-bucketed). 025-02 appends 4 entries (connections), 025-03 appends 1 (knowledge), 025-04 appends 3 (behavior), 025-05 appends 1 (workspace). Merge conflicts are deterministic and trivial — first to merge wins ordering, others rebase. |
| Route file shared by ALL 6 EPIC-025 stories — strict merge order required | High | Med | Per §2: 01 → 02 → 03 → 04 → 05 → 06. If branches merge out of order, the legacy block becomes inconsistent. Sprint orchestrator enforces. |
| `WorkspaceData` type defined in 025-01 needs all fields 025-02..05 resolvers will read; if 025-01 ships incomplete, every later story patches the type. | Med | Med | §5 below specifies the complete `WorkspaceData` shape verbatim. 025-01 must ship it fully populated even though only the foundation uses it directly. |
| Status resolver dispatch in 025-01 is invoked once per render — but `WorkspaceData` aggregation requires 7 useQuery calls. If any are loading, resolvers see `undefined` and crash. | Med | Med | WorkspaceShell must guard: render skeleton until ALL queries resolve, OR resolvers must accept partial data and return `'neutral'` for missing fields. Recommend: each resolver narrows on `data?.foo ?? defaultValue`. Document in 025-01 §5 below. |
| 025-02 KeySection re-skin breaks existing `KeySection.test.tsx` assertions on old card markup | Med | Low | Read `__tests__/KeySection.test.tsx` BEFORE re-skin; update class assertions in same commit; story DoD checks "no regression". |
| 025-04 AutomationsSection empty-state re-skin breaks existing `AutomationsSection.test.tsx` snapshot | Med | Low | Re-run test; update snapshot in same commit. Story §1.3 explicitly says "populated state preserved" so populated-state assertions stay green. |
| 025-05 model change `WorkspaceResponse + is_owner` propagates to `useWorkspaceQuery` consumers in unknown places | Low | Low | Optional field (`is_owner?: boolean`) — existing TS consumers don't break. Grep `frontend/src` for `Workspace` type usages confirms low blast radius. |
| 025-05 BUG-002 regression: tightening DELETE could be perceived as "BUG-002 partial reversal" by future reviewers | Low | Med | Architect's helper named `assert_team_owner_role` — distinct from BUG-002's `assert_team_member`. Code comment in `delete_workspace` cites BUG-002 explicitly: "BUG-002 (2026-04-24) relaxed list/create to any team member; this DELETE owner-gate is additive, not a reversal." |
| 025-06 deletes legacy block but route's `loadGapiScript` (line 87) and `docTypeLabel` (line 109) and `sourceBadgeProps` (line 152) helpers are still referenced inside FilesSection — accidental deletion breaks Files | Med | High | 025-06 cutover MUST grep helpers before deleting; move them into `FilesSection.tsx` (or `frontend/src/lib/`) as part of cutover, not delete them. |
| ~~BUG-001 already shipped~~ | — | — | RESOLVED at kickoff — dropped from scope. |
| CR-001 conflict with 025-02 if Developer doesn't read latest ChannelSection.tsx after 025-02 merge | Med | Low | Pre-flight grep block in §3 CR-001 above mandates verifying the divider list is in place before adding the search input. |
| Token-ledger hook still not built — 4th sprint without cost capture | High | Low | Reporter flags in REPORT.md §Meta. Not a sprint blocker. |

---

## 5. Reuse / extraction opportunities for STORY-025-01

**Highest-leverage section.** 025-01's prop shapes determine whether 025-02..05 can each land in <3hr or each pay a 1hr rework tax.

### 5.1 `ModuleSection` prop contract (frozen by 025-01)

```ts
export interface ModuleSectionProps {
  id: string;              // becomes anchor: id={`tm-${id}`}
  title: string;           // h2 text
  caption?: string;        // optional sub-headline
  action?: ReactNode;      // top-right slot (e.g. "Add file" button)
  children: ReactNode;     // module body
}
```

**Constraint for 025-02..05:** every section component (`SlackSection`, `DriveSection`, `KeySection`, `ChannelSection`, `FilesSection`, `PersonaSection`, `SkillsSection`, `AutomationsSection`, `DangerZoneSection`) renders ONLY the body; the `<ModuleSection>` wrapper is provided by `WorkspaceShell` based on the registry entry. **Section components do NOT render their own h2 or outer card border** — that's the `<ModuleSection>` job. This is non-negotiable to prevent double-bordering.

### 5.2 `WorkspaceData` type (frozen by 025-01)

```ts
// frontend/src/components/workspace/types.ts (NEW file in 025-01)
export interface WorkspaceData {
  workspace: Workspace;                              // is_owner, slack_domain, bot_persona, name, ...
  drive: { connected: boolean; email?: string };
  key: { has_key: boolean; provider?: string; key_hint?: string };
  channels: ChannelBinding[];                        // .filter(c => c.bound).length
  files: KnowledgeFile[];                            // .length
  skills: Skill[];                                   // .length
  automations: Automation[];                         // .length
  // Note: slackInstall is NOT a separate field — slack_team_id and slack_domain
  // both live on `workspace` (added by STORY-025-05). SlackSection reads from there.
}
```

WorkspaceShell aggregates this from existing hooks, gates on `isLoading` (renders skeleton until all 7 queries resolve), then passes `data: WorkspaceData` to every status resolver.

### 5.3 `ModuleEntry.statusResolver` contract (frozen by 025-01)

```ts
export type StatusResolver = (data: WorkspaceData) => ModuleStatus;
// Resolvers MUST tolerate missing nested fields (use ?? defaults) — never throw.
// Example: drive resolver = (d) => d.drive?.connected ? 'ok' : 'empty';
```

### 5.4 `StatusStrip` cell contract

```ts
export interface StatusCell {
  kicker: string;        // e.g. "PROVIDER"
  value: string;         // e.g. "OpenAI"
  caption?: string;      // e.g. "sk-proj-…G7vT"
}
```

Strip rendered cells are NOT bound to registry entries (they're aggregated metadata). 025-01 hard-codes the 4-cell builder using `WorkspaceData`. 025-02..05 do NOT touch StatusStrip.

### 5.5 `StickyTabBar` group-pill contract

```ts
// Per group, computed from registry entries in that group:
{ id: ModuleGroup; label: string; okCount: number; total: number }
// okCount = entries.filter(e => e.statusResolver(data) === 'ok').length
// total   = entries.length
```

### 5.6 Avatar tile pattern (used by 025-02 SlackSection + DriveSection, 025-03 FilesSection rows do NOT use this)

Recommend 025-02 extract a small helper inside `SlackSection.tsx` (since 025-02 introduces both Slack and Drive at the same time) and re-export it for `DriveSection` and `AutomationsSection.tsx` (025-04 empty state):

```tsx
export function ModuleAvatarTile({ icon: Icon }: { icon: LucideIcon }) {
  return (
    <div className="h-10 w-10 rounded-md bg-slate-100 flex items-center justify-center">
      <Icon className="h-5 w-5 text-slate-500" />
    </div>
  );
}
```

### 5.7 Divider-list pattern (025-02 Channels, 025-03 Files, 025-04 Skills)

Frozen markup:

```tsx
<ul className="divide-y divide-slate-100">
  {items.map(item => (
    <li key={item.id} className="group flex items-center justify-between gap-3 px-4 py-3">
      {/* left: icon + label */}
      {/* right: action (hover-revealed where applicable: opacity-0 group-hover:opacity-100 transition-opacity duration-150) */}
    </li>
  ))}
</ul>
```

025-03 documents the hover-reveal in code comment so 025-04 SkillsSection (no hover) doesn't accidentally inherit it.

---

## 6. Test inventory

| Story | Vitest target | Pytest target | Manual smoke |
|---|---|---|---|
| STORY-025-01 | 9 | 0 | 4 (per story §2.2) |
| STORY-025-02 | 5 | 0 | 4 |
| STORY-025-03 | 4 | 0 | 4 |
| STORY-025-04 | 5 | 0 | 3 |
| STORY-025-05 | 2 | 5 (DELETE matrix 4 + GET surface 1) | 7 |
| STORY-025-06 | 5 | 0 | 9 modules + deep links + 5 viewports |
| CR-001 | 3 | 0 | 1 (real ≥10-channel workspace) |
| ~~BUG-001~~ | — | — | RESOLVED at pre-flight |
| **TOTAL** | **33** | **5** | **32+** |

**Sprint plan §5 DoD called for ~25 Vitest** baseline (025-01 +9, 02 +4, 03 +4, 04 +5, 05 +2, 06 +5 = 29). After OQ resolutions: 025-02 grew to 5 (added "Slack degrades when domain absent" scenario for the OQ-3 contract); 025-05 pytest grew to 5 (added the OR-matrix coverage for OQ-2 = C); CR-001 +3 added by this blueprint. Total 33 Vitest + 5 pytest. Plus the BUG-002 regression check that re-runs existing `test_workspaces*` member LIST + CREATE access tests unmodified.

---

## 7. Open questions — RESOLVED at kickoff (2026-04-25)

All three OQs surfaced during W01 pre-flight resolved by human at sprint kickoff. Decisions propagated into STORY-025-02, STORY-025-05, BUG-001, and SPRINT-16.md.

### OQ-1 — BUG-001 status → **(a) Already shipped, drop from scope**

`AppNav.tsx` on `main` already contains all 3 implementation markers (verified 2026-04-25). BUG-001 frontmatter set to `status: Resolved`, `resolution: verified-shipped-no-commit`. SPRINT-16 scope reduced from 8 items to 7. Freed slot not re-allocated.

### OQ-2 — DELETE owner-gate semantics → **(C) Creator OR Team-Owner OR**

Either predicate is enough: workspace creator self-service preserved; Slack team-owner gains admin override. Error contract: 204 success, 403 when caller is team-member-but-neither (intra-team existence disclosure acceptable per ADR-024 spirit), 404 when caller is non-member or workspace missing. STORY-025-05 §1.2/§2.1/§3.2 rewritten with full code shape; W01 §3 STORY-025-05 sub-section updated; pytest count grew 4 → 5 to cover the OR matrix.

### OQ-3 — SlackSection caption data source → **(C) Add `slack_domain` to workspace GET**

Piggybacks on STORY-025-05's `is_owner` GET response extension (~2 LOC backend overhead). Frontend reads from existing `useWorkspaceQuery`; no new hook, no new endpoint. SlackSection renders ``${slack_team_id}${slack_domain ? ` · ${slack_domain}` : ''}`` defensively — falls back to team_id when domain missing. STORY-025-02 §1.2/§2.1 + STORY-025-05 §1.2/§2.1/§3.3 updated; W01 §3 STORY-025-02 sub-section + §5.2 WorkspaceData type + §6 inventory updated.

---

## 8. Granularity rubric pass

Per the protocol, each story is checked against signals: unrelated goals joined, >5 Gherkin scenarios, subsystems span, L4 complexity, splits/merges free at decomposition.

| Story | Goals | Gherkin | Subsystems | Complexity | Verdict |
|---|---|---|---|---|---|
| 025-01 | Single (build foundation) | 9 ⚠️ | Frontend only | L2 | **Keep.** 9 scenarios is over the rubric's "≤5" but they all belong to the foundation surface — splitting into "shell + scrollspy + deep-link" would force rework on the prop contracts in §5. The 9 scenarios map 1:1 to 9 distinct failure modes the foundation must handle; over-budgeting tests is the right tradeoff for a primitive that 4 follow-on stories consume. |
| 025-02 | Single (Connections group) | 4 | Frontend only | L2 | **Keep.** |
| 025-03 | Single (Knowledge group) | 4 | Frontend only | L1 | **Keep.** |
| 025-04 | Single (Behavior group) | 5 | Frontend only | L1 | **Keep.** Sprint plan owner asked: "should 025-04 split into Persona / Skills / Automation?" **Recommendation: NO split.** All three modules are extracted from the same route file in one sweep, share the divider-list pattern, and total <120 LOC of net change. Splitting would triple the merge-coordination overhead on `moduleRegistry.ts` and the route file with zero scope-clarity gain. The 5 scenarios test 3 distinct components — that's appropriate density. |
| 025-05 | Two (extract DangerZone + add owner gate — but tightly coupled) | 4 | Frontend + Backend | L1 | **Keep.** Backend gate is meaningless without UI hide; UI hide is dangerous without backend gate (security through obscurity). Coupled by design. |
| 025-06 | Two (mobile responsive + cutover) | 7 ⚠️ | Frontend only | L2 | **Keep, with caveat.** 7 scenarios is over rubric, but mobile and cutover are the same shipping action — both must land before legacy can be removed. Sprint plan §3 already accepted this. If S-16 runs hot, the splittable seam is: 025-06a (mobile responsive treatment, 3 scenarios) + 025-06b (delete legacy + SetupStepper, 4 scenarios). Recommend: ship as one story; if the diff exceeds 250 LOC of net change in the route file, retro-split before commit. |
| CR-001 | Single (search input) | 3 | Frontend only | L1 | **Keep.** |
| ~~BUG-001~~ | — | — | — | — | **Resolved at pre-flight** (OQ-1 = a). Already shipped on `main`. |

**No split or merge recommendations diverge from the sprint plan.**

---

## 9. Handoff

Order: **025-01 → (025-05 → 025-02 → 025-03 → 025-04 in parallel) → CR-001 (after 025-02) → 025-06**. 025-05 recommended first in the parallel batch so its `is_owner` + `slack_domain` GET fields are available when 025-02 SlackSection mounts. (025-02 is forward-compatible if 025-05 hasn't merged — caption falls back to team_id only — but the live caption only lights up after 025-05.) One commit per item on its own branch, merging into `sprint/S-16`. Grep `FLASHCARD.md` per the §0 tag bindings before each item. Typecheck + each story's Vitest scenarios green before handing to QA.

**Kickoff preflight check:** `cat .cleargate/sprint-runs/.active` must print `SPRINT-16` (verified 2026-04-25).

**All human gates closed at kickoff (2026-04-25):** OQ-1 → (a), OQ-2 → (C), OQ-3 → (C). Developer may proceed once 025-01 ships.
