# W01 — SPRINT-13 Milestone Plan (BUG-002 + STORY-018-05 + STORY-018-06)

**Architect:** session 2026-04-24 · **Sprint branch:** `sprint/S-13` off `main@a957832`
**Scope:** 1 backend auth bug + 2 frontend stories closing EPIC-018 dashboard UI.

## 0. Flashcard Sweep (do before coding)

Developer MUST grep `.cleargate/FLASHCARD.md` per item. Bindings that apply to W01:

- **`2026-04-12 #vitest #frontend` (critical):** jsdom lacks `HTMLDialogElement.showModal()`. Both 018-06 modals use the div-overlay pattern from `frontend/src/components/dashboard/CreateWorkspaceModal.tsx`. Never use `<dialog>`.
- **`2026-04-11 #frontend #recipe`:** all fetches go through typed fns in `frontend/src/lib/api.ts` called via TanStack `useQuery`/`useMutation`. No raw `fetch()`.
- **`2026-04-11 #vitest #test-harness`:** Vitest 2.x hoists `vi.mock` above local consts — wrap spies in `vi.hoisted(...)` if mocking hook modules.
- **`2026-04-13 #schema #test-harness`:** hermetic Supabase mocks don't validate column names; confirm BUG-002's new query columns (`teemo_slack_team_members.slack_team_id` + `.user_id`) against live migration SQL.
- **`2026-04-24 #frontend #epic-018` (recorded this session):** EPIC-018 dry-run endpoint is **POST `/automations/test-run`** with prompt-only body — NOT `/{aid}/dry-run`. The sprint-plan §2 wording is misleading; follow the stories + backend.

## 1. Red-Zone Surfaces

| File | Touched by | Note |
|---|---|---|
| `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` | 018-05 (mount section + 4 state stubs), 018-06 (mount 2 modals under stubs) | Merge order enforced: 018-05 before 018-06. |
| `frontend/src/lib/api.ts` | 018-05 adds 6 fns; 018-06 imports, adds none | Single writer in W01 (018-05). |
| `backend/app/api/routes/workspaces.py` + `channels.py` | BUG-002 only | Isolated from frontend work. |

## 2. Cross-Story Coupling & Reuse

- **018-06 is a pure consumer of 018-05.** No new entries in `api.ts` or `useAutomations.ts` during 018-06 — if the diff shows any, flag as scope drift.
- **WorkspaceCard state split:** 018-05 declares `addAutomationOpen`, `dryRunPrompt`, `dryRunName`, `dryRunOpen`. 018-06 mounts the modals under those same stubs — do not redeclare.
- **`getScheduleSummary()` mirrors backend `_schedule_summary()`** at `backend/app/agents/agent.py:177` — same output shape for all 5 occurrences so dashboard text matches Slack replies.
- **Delete two-click confirm:** copy `ChannelSection.tsx`'s state machine exactly; do not reinvent.

## 3. Per-Item Blueprints

### BUG-002 — `assert_team_owner` → `assert_team_member` (backend, L2, ~20 LoC net, parallel-eligible)

**Order — test FIRST (bug §5 is explicit):**
1. Create `backend/tests/test_workspaces_team_member_access.py` with 3 scenarios (member: GET → 200 empty, POST → 201; owner unaffected). Confirm RED on main.
2. Rename `assert_team_owner` → `assert_team_member` in `workspaces.py` (~lines 51–79). Switch query to `teemo_slack_team_members.eq("slack_team_id", team_id).eq("user_id", user_id)`. Keep 403 detail text.
3. Update 2 call sites: `list_workspaces`, `create_workspace`.
4. Mirror in `channels.py`: `_assert_slack_team_owner` → `_assert_slack_team_member`, 1 call site in `list_slack_channels`.
5. Docstring sweep: "owner" → "member" on affected routes (bug §6.3).
6. Confirm GREEN; full `pytest` stays green.

**Do-not-touch (bug §4):** `_assert_workspace_owner` helpers in `keys.py` / `drive_oauth.py` / `automations.py` / `channels.py` / `knowledge.py` (row-level creator scope per ADR-024); `slack_oauth.py` callback writes; frontend.

**QA proof-of-fix:** check out pre-fix commit → new test RED; fix commit → GREEN. This is the signature verification.

### STORY-018-05 — Automations Section + History Drawer (frontend, L2)

**New files:**
- `frontend/src/types/automation.ts` — all types per R1 + `getScheduleSummary()` (mirror backend `_schedule_summary`).
- `frontend/src/hooks/useAutomations.ts` — 6 TanStack hooks per R3. Keys: `['automations', workspaceId]`, `['automationHistory', workspaceId, automationId]`. Mutations invalidate the list key.
- `frontend/src/components/workspace/AutomationsSection.tsx` per R4 (delete = two-click, copied from `ChannelSection`).
- `frontend/src/components/workspace/AutomationHistoryDrawer.tsx` per R5 (inline slide-in, not portal — no jsdom dialog concerns).

**Modified:**
- `frontend/src/lib/api.ts` — 6 fns per R2. `testRunAutomation` → `POST /api/workspaces/{id}/automations/test-run` with body `{prompt, timezone?, description?}`.
- `frontend/src/routes/app.teams.$teamId.$workspaceId.tsx` — mount `<AutomationsSection />` and declare the 4 state stubs 018-06 needs.

**Tests (7 min, one per Gherkin):** empty state; card renders name/summary/Active badge/channel chip/4 buttons; toggle → mutation + Paused; delete → two-click + row gone; history click → drawer with `automationId`; drawer lists 3 rows newest-first; expand reveals `generated_content`.

**Watch-outs:** match the object-shape `useQuery` pattern used by `useWorkspaces.ts` et al.; no "Run Now" button (1.3 out-of-scope).

### STORY-018-06 — Add Modal + Schedule Builder + Dry Run Modal (frontend, L2)

**New files:**
- `AddAutomationModal.tsx` — div-overlay modal, form + schedule builder + channel picker + inline `DryRunResultInline` Preview (R1–R4).
- `DryRunModal.tsx` — div-overlay modal; fires `useTestRunMutation.mutate(prompt)` on open.

**Modified:** `app.teams.$teamId.$workspaceId.tsx` — mount both modals under 018-05's state stubs. **No** `api.ts` or `useAutomations.ts` edits.

**Schedule payload — MUST match `_AUTOMATIONS_PROMPT_SECTION` (backend/app/agents/agent.py:124–143):**

| Occurrence | Shape |
|---|---|
| Daily | `{"occurrence":"daily","when":"HH:MM"}` |
| Weekdays | `{"occurrence":"weekdays","when":"HH:MM"}` |
| Weekly | `{"occurrence":"weekly","when":"HH:MM","days":[0..6]}` (0=Sun … 6=Sat) |
| Monthly | `{"occurrence":"monthly","when":"HH:MM","day_of_month":1..31}` |
| Once | `{"occurrence":"once","at":"ISO8601 future"}` |

Top-level `schedule_type` = `"once"` if occurrence is `"once"`, else `"recurring"` (R2).

**Tests (8 min, one per Gherkin):** empty Name blocks; no channel blocks; past `once` blocks; success closes + invalidates; weekly no-days blocks; Preview fires `useTestRunMutation` with spinner → inline output; DryRunModal auto-fires on open; `channelBindings=[]` warns + disables Submit.

**jsdom rule (enforce):** div-overlay for both modals. `.showModal()` silently no-ops under Vitest — tests would green on a modal that never rendered. Reset all form state in `onClose` (DoD §4.2).

## 4. Sequencing

1. **BUG-002** (parallel-eligible, test-first) — can ship day 1.
2. **STORY-018-05** — creates API + hook surface.
3. **STORY-018-06** — consumer of 018-05; merges last.

One commit per item into `sprint/S-13`; `sprint/S-13` → `main` at sprint close.

## 5. Cross-Item Risks (beyond sprint plan §5)

| Risk | Like. | Impact | Mitigation |
|---|---|---|---|
| 018-06 duplicates API fns/hooks from 018-05 | Med | Low | This plan fixes 018-06 as pure consumer; Architect flags `api.ts`/`useAutomations.ts` diffs. |
| `getScheduleSummary()` drifts from backend `_schedule_summary()` | Med | Med | Same 5 shapes, same tz labels; cross-read `agent.py:177`. |
| Dry-run body shape | Low | Med | `{prompt, timezone?, description?}`, no `automation_id`; confirmed at `automations.py:180,376`. |
| BUG-002 hermetic mock skips column validation | Med | Med | Cross-check migration SQL for `teemo_slack_team_members(slack_team_id, user_id, role)` during test-first step. |

## 6. Out-of-Scope (sprint plan §7)

EPIC-024 leftovers, BUG-001 nav polish, automations `execute_async` refactor, EPIC-018 scheduled-run observability — all deferred.

## 7. Handoff

Start with BUG-002 (fastest unblock, independent). Then 018-05 full stack. Then 018-06. Grep FLASHCARD.md with the tags above before each item. Typecheck + relevant test suite must be green before handing to QA.
