/**
 * KeySection — BYOK key management section for a workspace.
 *
 * STUB: This component is implemented by STORY-008-01 (KeySection).
 * This placeholder exists so the build succeeds while STORY-008-04 is merged first.
 * Replace this file with the real implementation when STORY-008-01 merges.
 */

/** Props accepted by KeySection. */
export interface KeySectionProps {
  /** The workspace ID to manage keys for. */
  workspaceId: string;
  /** The Slack team ID (passed through from parent). */
  teamId: string;
}

/**
 * KeySection — stub placeholder for BYOK key management.
 *
 * @param props - KeySectionProps
 */
export function KeySection({ workspaceId: _workspaceId, teamId: _teamId }: KeySectionProps) {
  return null;
}
