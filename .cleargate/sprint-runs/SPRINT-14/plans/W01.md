# W01 — SPRINT-14 Milestone Plan (STORY-024-05 + STORY-024-04 + STORY-018-07 + STORY-018-08)

**Architect:** session 2026-04-24 · **Sprint branch:** `sprint/S-14` off `main@4325ad1`
**Scope:** 2 backend test-infra fixes (EPIC-024) + 2 timezone-awareness stories (EPIC-018).

> Sprint plan: [`../../delivery/pending-sync/SPRINT-14.md`](../../../delivery/pending-sync/SPRINT-14.md). This plan adds the concrete blueprint, test recipes, flashcard bindings, and reuse hooks the Developer agent must follow.

## 0. Flashcard Sweep (do before coding)

Developer MUST grep `.cleargate/FLASHCARD.md` per item. Bindings that apply to W01:

- **`2026-04-24 #test-harness #fastapi` (load-bearing for 024-05):** `with TestClient(app) as client:` deadlocks under pytest-asyncio auto mode — lifespan spawns drive/wiki/automation cron loops. Use bare `TestClient(app)` (no context manager). Canonical pattern: `backend/tests/test_workspaces_team_member_access.py:317` and `backend/tests/test_auth_routes.py:72`.
- **`2026-04-13 #schema #test-harness` (load-bearing for 024-04):** hermetic Supabase mocks don't validate column names — before adding the `teemo_slack_teams` branch, cross-read the actual migration SQL for the row shape. `FAKE_SLACK_TEAM_ROW` already exists at `test_channel_binding.py:72` and `test_channel_enrichment.py:64` — reuse it, don't redefine.
- **`2026-04-13 #drive` (load-bearing for 024-04 R2):** backend uses `drive.readonly` (not `drive.file`) for refresh-token flows. Assertion rename must match.
- **`2026-04-24 #vitest #test-harness` (load-bearing for 018-07):** jsdom `Intl.DateTimeFormat().resolvedOptions().timeZone` is deterministic but defaults to the host zone — use `vi.spyOn(Intl.DateTimeFormat.prototype, 'resolvedOptions')` to inject per-test fixtures. Don't rely on process env.
- **`2026-04-12 #vitest #frontend` (applies to 018-07):** jsdom has no `HTMLDialogElement.showModal()`. `AddAutomationModal.tsx` already uses the div-overlay pattern from SPRINT-13 — do not regress to `<dialog>`.
- **`2026-04-24 #llm #prompt` (load-bearing for 018-08 R3/R4):** never gate a prompt section on "entity exists" — the keyword hints go in unconditionally. The standing tz rule goes in the always-on preamble, not inside `## Scheduled Automations`.
- **`2026-04-24 #llm #slack` (load-bearing for 018-08 manual verification):** thread-history anchoring beats a freshly-fixed system prompt. When spot-testing the tz rule live, open a **fresh** DM/thread; the prior thread's assistant messages will make the old behavior persist even with the new prompt.
- **`2026-04-24 #reporting #hook`:** `.cleargate/sprint-runs/.active` already holds `SPRINT-14` (verified). Do not regress this at kickoff — without it the SubagentStop hook drops every token-ledger row.

## 1. Red-Zone & Shared Surfaces

| File | Touched by | Note |
|---|---|---|
| `backend/tests/test_channel_binding.py` | **024-05** (drop `with`-context at lines 115, 133) AND **024-04** (add `teemo_slack_teams` branch inside `_table`) | **Sequential merge enforced — 024-05 merges FIRST.** 024-04 Developer MUST `git rebase origin/sprint/S-14` onto merged 024-05 before starting. See §4. |
| `backend/app/agents/agent.py` | **018-08 only this sprint**, but repo-wide red-zone (EPIC-007, EPIC-015, EPIC-018 all land here; SPRINT-13 warned on it explicitly). | Developer edits ONLY these three surfaces: `AgentDeps` class (line 223), `_build_system_prompt` (line 346), `create_automation` tool (line 613, signature at line 619). See §3.4 below for a line-by-line "safe to edit / off-limits" table. |
| `backend/app/services/slack_dispatch.py` | **018-08 only** | Only the `users_info` block at ~L337–L345 and the `build_agent(...)` call at L353–L357. Do not touch mention-stripping, thread-history fetch, or Block Kit assembly. |
| `frontend/src/components/workspace/AddAutomationModal.tsx` | **018-07 only** | Lines 387–397 (state init) and 417–428 (`resetForm`). The rest of the component is out-of-scope for this story. |

No surface is hit by 3+ stories — no sprint-wide red-zone declaration needed.

## 2. Sequencing Recap (mirror of SPRINT-14.md §3)

1. **STORY-024-05** first — unblocks the default pytest collection path.
2. **STORY-024-04** second — strictly sequential because of the `test_channel_binding.py` shared edit. Rebase rule in §4.
3. **STORY-018-07** parallel-eligible from day 1 — pure frontend, no backend surface overlap.
4. **STORY-018-08** after 024-05 lands (not blocked by 024-04) — its tests in `test_automation_tools.py` / `test_slack_dispatch.py` aren't in the 024-05 hang list, but bundling after the full-suite unblock gives QA clean signal.

## 3. Per-Story Blueprints

### STORY-024-05 — TestClient lifespan unblock (backend, L2, P0)

**Commit prefix:** `refactor(epic-024): STORY-024-05 drop TestClient lifespan trigger in 4 hanging modules`

**Files (exhaustive — verified via grep during architecture review):**

| File | Occurrences to convert | Notes |
|---|---|---|
| `backend/tests/test_workspace_routes.py` | line 114 (`with TestClient(app) as client:`), line 895 (`with TestClient(app) as unauthenticated_client:`) | Two distinct fixtures — both need the `with` dropped. Move any `app.dependency_overrides.clear()` from the `with` block into a `try/finally`. |
| `backend/tests/test_channel_binding.py` | line 115, line 133 | **Shared file with 024-04.** Only the `with`-context change lands here. |
| `backend/tests/test_channel_enrichment.py` | line 112 | Single occurrence. |
| `backend/tests/test_automations_routes.py` | line 250 (fixture `app_client`), line 271 (fixture `app_client_non_owner`) | These ALREADY use `TestClient(app, raise_server_exceptions=False)` positionally — the hang comes from the `with ... as client:` wrapping, not the positional arg. Drop the `with`, then explicitly `yield client, mock_sb` followed by `app.dependency_overrides.clear()` outside any context manager. |

**Canonical conversion (from `test_workspaces_team_member_access.py:317`):**

```python
# Use TestClient WITHOUT context manager — avoids triggering lifespan cron tasks.
client = TestClient(app, raise_server_exceptions=False)
try:
    with patch("app.api.routes....", return_value=mock_sb):
        resp = client.get(...)
finally:
    app.dependency_overrides.clear()
```

**Per R5:** add a one-line module docstring header to each edited module, e.g. `"""... Uses bare TestClient (no context manager) — see flashcard 2026-04-24 #test-harness #fastapi."""`

**Per R4:** The flashcard is already logged (2026-04-24). Developer verifies the line is present post-edit; no new card needed unless a new wrinkle emerges during the refactor.

**Test recipe (QA re-runs literally):**

```bash
# Pre-refactor count capture (story §2.2 step 1)
for f in backend/tests/test_workspace_routes.py backend/tests/test_channel_binding.py \
         backend/tests/test_channel_enrichment.py backend/tests/test_automations_routes.py; do
  git stash && backend/.venv/bin/pytest "$f" --collect-only -q | tail -1; git stash pop
done

# Targeted 4-file run (story §2.1 scenario 1; must complete <60s, exit 0)
backend/.venv/bin/pytest \
  backend/tests/test_workspace_routes.py \
  backend/tests/test_channel_binding.py \
  backend/tests/test_channel_enrichment.py \
  backend/tests/test_automations_routes.py -v

# Full suite (story §2.1 scenario 3; no hang)
backend/.venv/bin/pytest backend/tests/ -q
```

Fixtures/mocks: no new ones. Existing `_make_*_supabase_mock` factories continue to work unchanged.

**Reuse:** Canonical pattern lifted from `test_workspaces_team_member_access.py:317`. The `app_client`/`app_client_non_owner` fixtures in `test_automations_routes.py` become generator-style fixtures without the `with` wrap — see `test_auth_routes.py:64-72` for the equivalent no-context pattern.

**Flashcard hits (Dev re-reads before implementation):**
- 2026-04-24 · `#test-harness #fastapi` (primary)
- 2026-04-12 · `#fastapi #test-harness` (module-level `httpx` monkeypatching — adjacent concern)

---

### STORY-024-04 — Fix legacy backend test tech-debt (backend, L1, P0, blocked-by 024-05)

**Commit prefix:** `fix(epic-024): STORY-024-04 unbreak 4 legacy test modules`

**Files (exhaustive):**

| File | Fix |
|---|---|
| `backend/tests/test_config_google.py` | In `test_defaults_to_empty_string_when_not_set`, add a monkeypatch that strictly unsets/empties `GOOGLE_PICKER_API_KEY` in the test env so the assertion `settings.google_picker_api_key == ""` isn't contaminated by a populated `.env`. Do NOT change the asserted value — fix the isolation. |
| `backend/tests/test_drive_oauth.py` | Rename `test_initiate_drive_connect_url_contains_drive_file_scope` → `test_initiate_drive_connect_url_contains_drive_readonly_scope`. Update the substring assertion from `drive.file` to `drive.readonly`. Flashcard 2026-04-13 `#drive` explains why. |
| `backend/tests/test_channel_binding.py` | In the `_table(name: str)` helper at line 159 (and the other `_table` factories at lines 225, 283, 329, 387, 437, 487 — grep verified 7 copies in this file), add `elif name == "teemo_slack_teams":` returning a MagicMock whose `.select(...).eq(...).limit(...).execute()` chain yields `_make_execute_result([FAKE_SLACK_TEAM_ROW])`. Reuse the already-imported `FAKE_SLACK_TEAM_ROW` constant at line 72 — do NOT redefine. |
| `backend/tests/test_agent_factory.py` | Same `teemo_slack_teams` mock-seeding fix. Grep for each local `_table` / `get_supabase()` mock factory and add the branch. Column set must match what `build_agent` selects (`bot_token_encrypted` / `slack_team_id` — verify against latest migration per flashcard 2026-04-13 `#schema #test-harness`). |

**Out-of-scope reminder (story §1.3):** no app-code changes. If the Developer finds themselves editing under `backend/app/`, STOP and raise scope creep.

**Test recipe (QA re-runs literally):**

```bash
# Targeted 4-file run (story §2.2)
backend/.venv/bin/pytest \
  backend/tests/test_config_google.py \
  backend/tests/test_drive_oauth.py \
  backend/tests/test_channel_binding.py \
  backend/tests/test_agent_factory.py -v

# Regression guard: full backend suite still clean (post-024-05 this is meaningful)
backend/.venv/bin/pytest backend/tests/ -q
```

Fixtures/mocks: reuse `FAKE_SLACK_TEAM_ROW` and `_make_execute_result`. No new helpers.

**Reuse:** `FAKE_SLACK_TEAM_ROW` (defined at `test_channel_binding.py:72` and `test_channel_enrichment.py:64`). The `teemo_slack_teams` select/eq/limit chain used in later tests in the same file (e.g. line 495) is the canonical shape — copy it.

**Flashcard hits:**
- 2026-04-13 · `#schema #test-harness` (primary — mock column validation)
- 2026-04-13 · `#drive` (R2 scope rename)
- 2026-04-11 · `#schema #fastapi` (Tee-Mo table prefix — `teemo_slack_teams` is correct)

---

### STORY-018-07 — Dashboard modal browser-tz default (frontend, L1, P1, parallel-eligible)

**Commit prefix:** `feat(epic-018): STORY-018-07 honor browser timezone in AddAutomationModal`

**Files:**

| File | Change |
|---|---|
| `frontend/src/components/workspace/AddAutomationModal.tsx` | **Lines 391–397 (state init):** replace the `Object.fromEntries(TIMEZONES.map(...))` gate with a module-level `DETECTED_TZ` constant computed once via `try { Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC' } catch { 'UTC' }`. `useState(() => DETECTED_TZ)`. **Line 422 (`resetForm`):** change `setTimezone('UTC')` → `setTimezone(DETECTED_TZ)`. **Dropdown options:** build `const tzOptions = TIMEZONES.includes(DETECTED_TZ) ? TIMEZONES : [DETECTED_TZ, ...TIMEZONES]` and render from `tzOptions` instead of `TIMEZONES` directly. Document the ordering choice in a comment. |
| `frontend/src/components/workspace/__tests__/AddAutomationModal.test.tsx` | Add 2 new tests (story §4.1). Place them alongside existing tests; follow the modal's existing render pattern. |

**Out-of-scope (story §1.3):** no full IANA selector. No edit-path behavior yet. No shape change to `createAutomation` payload.

**Test recipe (QA re-runs literally):**

```bash
# Targeted modal suite
cd frontend && npx vitest run src/components/workspace/__tests__/AddAutomationModal.test.tsx

# Full frontend suite — no regressions elsewhere
cd frontend && npm test
```

Fixtures/mocks the Dev must set up:
- `vi.spyOn(Intl.DateTimeFormat.prototype, 'resolvedOptions').mockReturnValue({ timeZone: 'Europe/Berlin', ...restOfDefault })` for the curated-zone test.
- Same spy returning `{ timeZone: 'America/Phoenix' }` for the non-curated test.
- Spy returning `{ timeZone: '' }` or throwing for the fallback scenario (Gherkin 3). Use `beforeEach`/`afterEach` to restore.

**Reuse:**
- The div-overlay modal shell in `AddAutomationModal.tsx` (R4 out-of-scope warning — do not touch).
- Existing `TIMEZONES` constant export location (grep it; do not redefine).
- The test-file scaffolding in `__tests__/AddAutomationModal.test.tsx` (rendering, react-query wrapper, store mocks) — extend, don't rewrite.
- Story §3.2 cites `new_app/frontend/src/components/settings/AutomationsTab.tsx:94` as the `DETECTED_TZ` precedent — **that file does not exist in this working tree** (verified). Use the pattern described in R1 verbatim; do not try to copy from a missing file.

**Flashcard hits:**
- 2026-04-24 · `#vitest #test-harness` (Intl spy pattern)
- 2026-04-12 · `#vitest #frontend` (no `<dialog>`, div-overlay only)
- 2026-04-11 · `#frontend #recipe` (no raw `fetch()` — still applies across the component)

---

### STORY-018-08 — Agent knows scheduler's timezone (backend, L2, P1)

**Commit prefix:** `feat(epic-018): STORY-018-08 thread sender_tz through slack_dispatch → AgentDeps → create_automation`

**Files:**

| File | Change |
|---|---|
| `backend/app/services/slack_dispatch.py` | Around line 338 inside the existing `try:` block that calls `client.users_info(user=sender_user_id)`: after resolving `sender_name`, read `sender_tz = (user_data.get("tz") or "").strip() or "UTC"`. On the `except Exception` branch (line 344), set `sender_tz = "UTC"` explicitly. Then pass `sender_tz=sender_tz` as a kwarg into `build_agent(...)` at line 353. |
| `backend/app/agents/agent.py` — `AgentDeps` (line 223) | Add field `sender_tz: str = "UTC"` with docstring line matching the R2 precedent (cite `citations`). Default is LOAD-BEARING — hand-rolled `SimpleNamespace` fakes in tests must keep compiling. |
| `backend/app/agents/agent.py` — `_build_system_prompt` (line 346) | Add kwarg `sender_tz: str = "UTC"`. After the existing UTC `current_time_line`: when `sender_tz != "UTC"`, compute `now_local = datetime.now(ZoneInfo(sender_tz)).strftime("%H:%M on %Y-%m-%d")` and append R3's exact line. When `sender_tz == "UTC"`, append the softer variant verbatim. Append R4's standing rule to the Rules block (next to "Be concise and helpful") — **always**, regardless of `sender_tz` value (per flashcard 2026-04-24 `#llm #prompt`: never gate prompt hints on entity existence). |
| `backend/app/agents/agent.py` — `create_automation` tool (line 613) | Keep the `timezone: str = "UTC"` signature argument (tool schema stays stable). In the function body, **before** constructing the automation row, do `if timezone == "UTC": timezone = getattr(ctx.deps, "sender_tz", "UTC")`. This preserves explicit overrides (user says "9am New York" → agent passes `timezone="America/New_York"` → no override). Return value should include the final tz so the agent can cite it (R5). |
| `backend/app/agents/agent.py` — `update_automation` tool | **NO CHANGE** (R6). Confirm in code review that no default-tz logic leaks into update. |
| `backend/app/agents/agent.py` — `build_agent` (line 837) | Add `sender_tz: str = "UTC"` kwarg, store on `AgentDeps`, thread into `_build_system_prompt(...)` at line 979. |
| `backend/tests/test_automation_tools.py` | Add 4 unit tests (story §4.1). Fixture that constructs `AgentDeps(..., sender_tz="America/Los_Angeles")` — use the existing `AgentDeps` construction pattern in the file; do NOT build a new mock class. |
| `backend/tests/test_slack_dispatch.py` | Add 2 integration tests: (a) happy path where `users_info` returns `{"user": {"tz": "America/Los_Angeles", ...}}` and asserts `build_agent` was called with `sender_tz="America/Los_Angeles"`; (b) `users_info` raises → `sender_tz="UTC"` + no 500. Use `AsyncMock` for `client.users_info` patch. |

**Off-limits inside `agent.py` (red-zone guard):**

| Line range | Surface | Why off-limits |
|---|---|---|
| 1–222 | imports, module docstring, constants, `_build_pydantic_ai_model` | Not in scope — any diff here is scope creep. |
| 244–344 | `AgentDeps` fields beyond `sender_tz`, skill-tool setup | Don't reorder existing fields; append `sender_tz` after `citations`. |
| 396–598 | Wiki/document/skill tool bodies | Unrelated. |
| 683–836 | `list_automations`, `update_automation`, `delete_automation` tool bodies | R6 explicitly forbids auto-rewriting tz in `update_automation`. |
| 888–978 (inside `build_agent`) | Workspace row fetch, skill catalog, wiki fetch, tool wiring | Only the final `_build_system_prompt(...)` call needs `sender_tz=...` threaded. Do not refactor the surrounding steps. |

Safe-to-edit line ranges: **223–243** (AgentDeps field add), **346–395** (`_build_system_prompt` signature + body additions), **613–~680** (`create_automation` default-source change + return-string tweak), **the single call site of `_build_system_prompt` at line 979**.

**Test recipe (QA re-runs literally):**

```bash
# Targeted — new tests + regression guard on these two modules
backend/.venv/bin/pytest \
  backend/tests/test_automation_tools.py \
  backend/tests/test_slack_dispatch.py -v

# Regression guard — agent module has wide blast radius
backend/.venv/bin/pytest backend/tests/test_agent_factory.py backend/tests/test_slack_dispatch_citation_kwargs.py -v

# Full suite
backend/.venv/bin/pytest backend/tests/ -q
```

Fixtures/mocks the Dev must set up:
- `AgentDeps` construction: reuse the existing pattern (grep the file for `AgentDeps(`).
- `users_info` mock: `AsyncMock(return_value={"user": {"tz": "America/Los_Angeles", "real_name": "...", "profile": {...}}})`.
- `ZoneInfo` is stdlib (Python 3.9+) — no new dependency. If the codebase already imports it (grep confirms), reuse that import site.

**Reuse:**
- `citations` field on `AgentDeps` is the documented precedent for a defaulted-with-fallback field (story R2 cites it; docstring at line 235–240). Mirror the docstring style.
- `sender_name` resolution (slack_dispatch.py:341–343) is the precedent for "pluck optional field from `users_info` with safe fallback" — same pattern for `sender_tz`.
- `_build_system_prompt` already accepts optional sections via kwargs (documents, wiki_pages, automations, bound_channels) — `sender_tz` slots into the same signature style. Do not rewrite the function — append one kwarg and two blocks.

**Flashcard hits:**
- 2026-04-24 · `#llm #prompt` (standing rule is always-on, not keyword-gated)
- 2026-04-24 · `#llm #slack` (fresh-thread rule for manual verification)
- 2026-04-24 · `#llm #pydantic-ai` (tool-schema stability — `timezone` param stays in the pydantic-ai schema exposed to the model)

## 4. Shared-Surface Merge Rule (`backend/tests/test_channel_binding.py`)

Both 024-05 (drop `with`-context at lines 115, 133) and 024-04 (add `teemo_slack_teams` branch inside `_table` at line 159 and the other 6 `_table` factories) edit this file.

**Sequencing (hard rule, per SPRINT-14.md §4):**

1. Developer lands **STORY-024-05** on `sprint/S-14` first.
2. Before starting 024-04, Developer **rebases** their 024-04 branch onto the updated `sprint/S-14`:
   ```bash
   git fetch origin
   git checkout story/STORY-024-04-legacy-tests
   git rebase origin/sprint/S-14
   # resolve any conflicts inside test_channel_binding.py by keeping BOTH changes:
   #   - 024-05's no-context-manager pattern at lines 115, 133
   #   - 024-04's new teemo_slack_teams branch inside _table factories
   ```
3. QA verifies both diffs together: run the full `pytest backend/tests/test_channel_binding.py -v` post-rebase and confirm zero regression.
4. Team Lead inspects the combined diff in review before merging 024-04 into `sprint/S-14`.

**Conflict triage:** If the rebase produces conflicts OUTSIDE `test_channel_binding.py`, that is scope creep — stop and escalate.

## 5. Cross-Item Risks (beyond SPRINT-14.md §5)

| Risk | Like. | Impact | Mitigation |
|---|---|---|---|
| 024-05 conversion leaves `app.dependency_overrides` un-cleaned on test failure | Low | Med | Canonical pattern uses `try/finally` — Developer must not just delete the `with` and move on. Architect reviews each conversion. |
| 024-04 seeds `FAKE_SLACK_TEAM_ROW` for 7 `_table` factories but misses one | Med | Low | After the fix, grep `def _table(name` in `test_channel_binding.py` — count must equal the number of `elif name == "teemo_slack_teams":` branches added. |
| 018-07 `DETECTED_TZ` computed at module scope before `Intl` is stubbed in tests | Med | Med | Compute `DETECTED_TZ` inside the component function (not module scope) OR use `useState` initializer so the spy in each test runs before first read. Architect inspects the diff for this. |
| 018-08 missing `ZoneInfo` import or runtime tz value not in IANA DB (e.g. Slack returns a custom label) | Low | Med | Wrap `datetime.now(ZoneInfo(sender_tz))` in a try/except falling back to UTC variant. R7 requires "no 500, no retry storm" — add this defense. |
| 018-08 prompt drift — standing rule bleeds into non-scheduling replies | Med | Med | Wording in R4 is deliberate ("Whenever you schedule, confirm, or reason about a specific time"). QA adds one non-scheduling prompt fixture and confirms the reply does not spuriously cite tz. |
| 024-04 `teemo_slack_teams` row-shape drift vs. live migration | Med | Med | Flashcard 2026-04-13 `#schema #test-harness` — cross-check `backend/migrations/` for latest `teemo_slack_teams` columns before shipping. |

## 6. Out-of-Scope (mirror SPRINT-14.md §7)

- Root-cause `main.py` lifespan fix (env-gated cron disable) — deferred after 024-05 validates.
- STORY-024-02 (background worker locks) — SPRINT-15.
- BUG-001 nav glassmorphism polish — P3, side PR if velocity allows.
- Dashboard-side workspace default tz setting — 018-08 §1.3.
- Full IANA searchable tz selector — 018-07 §1.3.
- EPIC-018 per-run Slack error channels — future CR.

## 7. Handoff

Order: **024-05 → 024-04 (rebase!) → 018-07 (parallel from day 1) → 018-08 (after 024-05 lands)**. One commit per story on its own branch, merging into `sprint/S-14`. Grep `FLASHCARD.md` with the tags listed under §0 and the per-story "Flashcard hits" block before each item. Typecheck + the story's test recipe must be green before handing to QA.

**Kickoff preflight check:** `cat .cleargate/sprint-runs/.active` must print `SPRINT-14`. (Verified 2026-04-24 — already set.) Without this, the SubagentStop hook drops every token-ledger row and the Reporter ends with an empty retrospective.
