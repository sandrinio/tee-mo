"""Tee-Mo service layer.

Contains business-logic services that are called by route handlers.
Services are kept separate from routes to make them independently
testable and to keep route files focused on HTTP concerns.
"""
