/**
 * SetupStepper — guided workspace setup wizard component.
 *
 * STUB: This component is implemented by STORY-008-02 (SetupStepper).
 * This placeholder exists so the build succeeds while STORY-008-04 is merged first.
 * Replace this file with the real implementation when STORY-008-02 merges.
 */

/** Props accepted by SetupStepper. */
export interface SetupStepperProps {
  /** The workspace ID for the setup flow. */
  workspaceId: string;
  /** The Slack team ID. */
  teamId: string;
}

/**
 * SetupStepper — stub placeholder for the workspace setup wizard.
 *
 * @param props - SetupStepperProps
 */
export function SetupStepper({ workspaceId: _workspaceId, teamId: _teamId }: SetupStepperProps) {
  return null;
}
